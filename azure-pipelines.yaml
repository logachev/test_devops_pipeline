# Docker image
pool:
  vmImage: 'Ubuntu 16.04'

variables:
  imageName: 'terraform-azurerm-postgresql:$(build.buildId)'

steps:
- script: bash test.sh $(client_id) $(client_secret) $(system.debug)
  displayName: 'build'
